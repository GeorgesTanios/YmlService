%YAML 1.3
---

afbidl: "0.1"
#** a modifier 
info:
  apiname:
  title:
  description:
  author:
  maintainer:
  homepage:
#** ****
tools:

    afb-genskel:
        scope: static
        prefix: req_
        postfix: _cb
        init: init_gps

    doc:
        id: gps-api
        keywords: gps
        author:
        version:
        src_prefix: api-gps
        chapters:
        - name: Abstract
          url: abstract.md
        - name: User Guide
          url: userguide.md

verbs:

  verb1:
    description: #what this function do
    request: $/schemas/ #concerned-schema
    reply:
      success: $/schemas/none
      set-state:
        #actionname : status [Yes || No]
  
  verb1:
    description: #what this function do
    request: $/schemas/ #---
    reply:
      success: $/schemas/ #---
      set-state:
        #actionname : status [Yes || No]
